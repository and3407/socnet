# Архитектура сервиса счетчиков непрочитанных сообщений

## Обзор

Сервис счетчиков непрочитанных сообщений разработан как отдельный микросервис на основе Lumen (PHP 8.4) для обеспечения высокой производительности при большой нагрузке на чтение. Сервис интегрируется с существующей системой чатов и обеспечивает консистентность данных через паттерн SAGA.

## Цели

1. **Высокая производительность чтения**: Оптимизация для частых запросов количества непрочитанных сообщений.
2. **Консистентность данных**: Гарантия соответствия счетчика реальному количеству непрочитанных сообщений.
3. **Масштабируемость**: Возможность горизонтального масштабирования.
4. **Интеграция с существующей системой**: Совместимость с основным приложением и сервисом диалогов.

## Архитектурная схема

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Основное      │     │   Сервис        │     │   Сервис        │
│   приложение    │────▶│   диалогов      │────▶│   счетчиков     │
│   (PHP 8.4)     │     │   (Lumen)       │     │   (Lumen)       │
└─────────────────┘     └─────────────────┘     └─────────────────┘
         │                       │                       │
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   PostgreSQL    │     │   RabbitMQ      │     │   Redis         │
│   (main db)     │     │   (очереди)     │     │   (кэш)         │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

## Компоненты системы

### 1. Сервис счетчиков (counter-service)
- **Технологии**: Lumen 10.x, PHP 8.4
- **Порт**: 9100
- **База данных**: PostgreSQL (counter_db)
- **Кэш**: Redis для хранения горячих счетчиков
- **Очереди**: RabbitMQ для асинхронной обработки событий

### 2. База данных счетчиков
**Схема:**
```sql
CREATE TABLE user_unread_counts (
    user_id UUID PRIMARY KEY,
    total_unread INTEGER DEFAULT 0,
    updated_at TIMESTAMP
);

CREATE TABLE dialog_unread_counts (
    user_id UUID,
    dialog_id UUID,
    unread_count INTEGER DEFAULT 0,
    PRIMARY KEY (user_id, dialog_id)
);
```

### 3. Redis кэш
- Кэширование счетчиков пользователей с TTL 60 секунд
- Стратегия "Cache-Aside" (Lazy Loading)
- Инвалидация при обновлении счетчиков

### 4. RabbitMQ очереди
- `counter.events` - события обновления счетчиков
- `counter.compensation` - события компенсации для SAGA

## Поток данных

### Сценарий отправки сообщения:
1. Пользователь отправляет сообщение через основной контроллер `DialogController::sendMessageAction`
2. Основное приложение вызывает сервис диалогов через `DialogServiceClient`
3. Сервис диалогов сохраняет сообщение и публикует событие `MessageSent` в RabbitMQ
4. Сервис счетчиков потребляет событие через consumer `ConsumeCounterEvents`
5. Сервис счетчиков:
   - Увеличивает счетчик непрочитанных для получателя
   - Обновляет кэш Redis
   - Публикует событие подтверждения
6. В случае ошибки запускается компенсационное действие (уменьшение счетчика)

### Сценарий чтения счетчиков:
1. Клиент запрашивает количество непрочитанных сообщений
2. Запрос идет в `CounterController::getUserUnreadCount`
3. Сервис проверяет кэш Redis:
   - При наличии: возвращает из кэша
   - При отсутствии: загружает из БД, сохраняет в кэш
4. Возвращает ответ в формате JSON

## Паттерн SAGA для обеспечения консистентности

### Проблема:
Необходимо гарантировать, что счетчик непрочитанных сообщений всегда соответствует реальному количеству непрочитанных сообщений в диалогах.

### Решение:
Используется паттерн SAGA с компенсирующими транзакциями:

1. **Основная транзакция**: Увеличение счетчика при получении нового сообщения
2. **Компенсирующая транзакция**: Уменьшение счетчика при откате (например, если сообщение не было сохранено)

### Реализация:
- События хранятся в RabbitMQ с идентификатором корреляции
- Каждое событие имеет статус (pending, completed, failed)
- Consumer обрабатывает события и отслеживает состояние SAGA
- При timeout или ошибке запускается компенсирующее событие

## API Endpoints

### GET /api/v1/counters/user/{userId}
**Описание**: Получение общего количества непрочитанных сообщений пользователя

**Ответ:**
```json
{
    "user_id": "uuid",
    "total_unread": 5,
    "updated_at": "2026-05-23T07:00:00Z"
}
```

### GET /api/v1/counters/user/{userId}/dialog/{dialogId}
**Описание**: Получение количества непрочитанных сообщений в конкретном диалоге

**Ответ:**
```json
{
    "user_id": "uuid",
    "dialog_id": "uuid",
    "unread_count": 3
}
```

### POST /api/v1/counters/health
**Описание**: Проверка здоровья сервиса

## Интеграция с основным приложением

### 1. Клиент сервиса счетчиков
Создан `CounterServiceClient` в основном приложении для взаимодействия с сервисом счетчиков:

```php
class CounterServiceClient {
    public function getUserUnreadCount(string $userId): int;
    public function incrementUnreadCount(string $userId, string $dialogId): void;
    public function resetUnreadCount(string $userId, string $dialogId): void;
}
```

### 2. WebSocket уведомления
При изменении счетчика отправляется WebSocket уведомление клиенту через существующий WebSocket сервер.

### 3. Аутентификация
Используется тот же Bearer token, что и в основном приложении. Сервис счетчиков проверяет токен через middleware.

## Оптимизации для высокой нагрузки

### 1. Кэширование
- Redis кэш для счетчиков пользователей
- TTL 60 секунд для баланса актуальности и производительности
- Пакетное обновление кэша

### 2. База данных
- Индексы на `user_id` и `(user_id, dialog_id)`
- Периодическая агрегация статистики
- Использование connection pooling

### 3. Асинхронная обработка
- Все обновления счетчиков через RabbitMQ
- Неблокирующие операции чтения
- Batch processing для накопленных событий

### 4. Мониторинг и метрики
- Prometheus метрики для количества запросов
- Логирование времени ответа
- Алерты при высокой задержке

## Развертывание

### Docker Compose
Сервис развертывается как часть docker-compose стека:

```yaml
counter-service:
  build: ./counter-service
  ports: ["9100:9100"]
  depends_on: [pgmain, redis, rabbitmq]

counter-consumer:
  build: ./counter-service
  command: php artisan counter:consume
  depends_on: [counter-service]
```

### Миграции базы данных
```bash
cd counter-service
php artisan migrate
```

## Тестирование

### 1. Юнит-тесты
- Тестирование бизнес-логики CounterUpdateService
- Тестирование моделей и репозиториев

### 2. Интеграционные тесты
- Тестирование API endpoints
- Тестирование интеграции с RabbitMQ

### 3. Нагрузочное тестирование
Использование `ab` (Apache Benchmark) для тестирования под нагрузкой:

```bash
ab -n 10000 -c 100 -H "Authorization: Bearer $TOKEN" \
  http://localhost:9100/api/v1/counters/user/$USER_ID
```

## Резервное копирование и восстановление

### 1. База данных
- Ежедневные снепшоты PostgreSQL
- Point-in-Time Recovery (PITR)

### 2. Redis
- RDB и AOF persistence
- Репликация для отказоустойчивости

## Мониторинг и алертинг

### 1. Метрики
- Количество запросов в секунду
- Время ответа (p50, p95, p99)
- Hit rate кэша Redis
- Очередь RabbitMQ

### 2. Логи
- Структурированные логи в JSON формате
- Централизованный сбор логов (ELK stack)

### 3. Алерты
- Высокая задержка (> 100ms)
- Низкий hit rate кэша (< 80%)
- Ошибки базы данных

## Заключение

Сервис счетчиков непрочитанных сообщений обеспечивает:
- Высокую производительность при большой нагрузке на чтение
- Консистентность данных через паттерн SAGA
- Легкую интеграцию с существующей системой
- Масштабируемость и отказоустойчивость

Архитектура позволяет обрабатывать тысячи запросов в секунду с минимальной задержкой, обеспечивая пользователям актуальную информацию о непрочитанных сообщениях.